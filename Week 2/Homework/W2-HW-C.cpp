#include <iostream>
using namespace std;

int main() {
    int a,b,c,d,e,f,g,h,i;
    cin >> a >> b >> c;  
    cin >> d >> e >> f;  
    cin >> g >> h >> i;  
    int m = a + e + i;  
    int n = c + e + g;    
    cout<<m<<n<<endl;
    return 0;
}